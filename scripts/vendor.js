const backdrop = document.getElementById('backdrop');
const getCall = document.getElementById('btn-get-call');
const popUpBooking = document.getElementById('pop-up-book');
const permanentBooking = document.getElementById('permanent-book');

const addPopUpForm = document.getElementById('pop-up-form');
const addPermanentForm = document.getElementById('permanent-form');

const inputPopUpForm = addPopUpForm.querySelectorAll('input');
const inputPermanentForm = addPermanentForm.querySelectorAll('input');

const getPrice = document.getElementById('get-price');
const masterCall = document.getElementById('master-call');
const knowPrice = document.getElementById('know-price');

const userInputsPop = addPopUpForm.querySelectorAll('input');
const userInputsPermanent = addPermanentForm.querySelectorAll('input');

const request = [];